{
  "name": "3d-landcover-map",
  "version": "1.1.0",
  "description": "Browser-based 3D terrain + land cover visualization using open-source datasets. No API keys, no backend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "geotiff": "^3.0.4",
    "three": "^0.160.0",
    "three-gpu-pathtracer": "^0.0.20"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
